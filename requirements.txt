# LegalDocuMan — Requirements
# ==========================================

# Core document processing
pdfplumber>=0.10.0,<1.0.0
python-docx>=0.8.11,<2.0.0
pandas>=1.3.0,<3.0.0
openpyxl>=3.0.9,<4.0.0
dateparser>=1.1.0,<2.0.0
python-dotenv>=1.0.0,<2.0.0

# OCR — Tesseract (required)
# Requires Tesseract OCR + poppler-utils installed on the host OS.
# See README for OS-specific install instructions.
pytesseract>=0.3.10,<1.0.0
pdf2image>=1.0.0,<2.0.0
Pillow>=9.0.0,<12.0.0

# Web application
Flask>=3.0.0,<4.0.0
gunicorn>=22.0.0,<24.0.0
Flask-SQLAlchemy>=3.1.0,<4.0.0
Flask-Migrate>=4.0.0,<5.0.0
Flask-Cors>=4.0.0,<6.0.0
psycopg2-binary>=2.9.0,<3.0.0
redis>=5.0.0,<7.0.0
rq>=2.0.0,<3.0.0
boto3>=1.34.0,<2.0.0

# Development and testing
pytest>=7.0.0,<9.0.0

# Optional NVIDIA OCR support (uncomment and set API key in .env to enable)
# requests>=2.25.0,<3.0.0

# Signature detection — RF-DETR visual model
rfdetr>=1.7.0
supervision>=0.23.0

# Small LM — FLAN-T5-small for document type + vendor extraction
# Upper bound matches rfdetr's required range (transformers >=5.1.0)
transformers>=4.40.0,<6.0.0
sentencepiece>=0.2.0
protobuf>=4.0.0