# Raw benchmark output

The benchmark driver writes one JSON file per revision, concurrency, and batch size here. Commit raw files only when all comparison runs were produced on the same host with the procedure in [`../README.md`](../README.md). CI uploads its runs as workflow artifacts instead of committing them.
